<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chúc mừng 20/10 💖</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      overflow: hidden;
      height: 100vh;
      background: radial-gradient(circle at top, #2c003e, #000000 80%);
      font-family: 'Comic Sans MS', cursive, sans-serif;
      position: relative;
      color: #fff;
    }

    h1 {
      position: absolute;
      top: 20px;
      width: 100%;
      text-align: center;
      color: #ffb6d9;
      text-shadow: 0 0 20px #ff4fa3, 0 0 40px #ff0099;
      font-size: 2em;
      animation: glow 2s infinite alternate;
      z-index: 5;
    }

    @keyframes glow {
      from { text-shadow: 0 0 15px #ff66cc, 0 0 25px #ff33aa; }
      to { text-shadow: 0 0 35px #ff0099, 0 0 60px #ff66cc; }
    }

    /* Hai ảnh nền mờ */
    .bg-image {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 80%;
      max-width: 600px;
      opacity: 0.15;
      filter: blur(3px);
      border-radius: 20px;
      z-index: 1;
    }

    .bg-image.second {
      top: 60%;
      left: 50%;
      opacity: 0.1;
      transform: translate(-50%, -50%) scale(1.2);
    }

    .message {
      position: absolute;
      color: #ffcce7;
      font-size: 1.5em;
      text-shadow: 0 0 10px #ff66cc, 0 0 25px #ff33aa;
      animation: fall linear;
      z-index: 10;
    }

    @keyframes fall {
      0% {
        transform: translateY(-10%);
        opacity: 1;
      }
      100% {
        transform: translateY(110vh);
        opacity: 0;
      }
    }

    .heart {
      position: absolute;
      color: #ff4fa3;
      font-size: 20px;
      opacity: 0.8;
      animation: floatHeart 6s linear infinite;
      z-index: 8;
    }

    @keyframes floatHeart {
      0% { transform: translateY(100vh) scale(0.8) rotate(0deg); opacity: 1; }
      100% { transform: translateY(-10vh) scale(1.3) rotate(360deg); opacity: 0; }
    }
  </style>
</head>
<body>
  <h1>💐 Chúc mừng ngày 20/10 💐</h1>

  <!-- Hai ảnh nền mờ (bạn thay link ảnh ở đây nếu muốn) -->
  <img src="https://i.imgur.com/bfd5e2c.png" class="bg-image" alt="Ảnh nền 1">
  <img src="https://i.imgur.com/f7ba0329.png" class="bg-image second" alt="Ảnh nền 2">

  <!-- Nhạc nền -->
  <audio id="music" autoplay loop>
    <source src="https://docs.google.com/uc?export=download&id=1mWmDk6Jlbk0Bq5vKHAdAmvVQei-ueKnF" type="audio/mp3">
  </audio>

  <script>
    const messages = [
      "chúc em ngày 20/10 vui vẻ nha 💖",
      "love you 💞",
      "❤️❤️",
      "chúc vợ 20/10 🌹",
      "anh yêu em 💋",
      "ngày 20/10 hạnh phúc nha 🌸"
    ];

    function createMessage() {
      const msg = document.createElement("div");
      msg.className = "message";
      msg.textContent = messages[Math.floor(Math.random() * messages.length)];
      msg.style.left = Math.random() * 90 + "vw";
      msg.style.animationDuration = (5 + Math.random() * 5) + "s";
      document.body.appendChild(msg);
      msg.addEventListener("animationend", () => msg.remove());
    }

    function createHeart() {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.textContent = "💖";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.fontSize = (10 + Math.random() * 20) + "px";
      heart.style.animationDuration = (4 + Math.random() * 4) + "s";
      document.body.appendChild(heart);
      heart.addEventListener("animationend", () => heart.remove());
    }

    setInterval(createMessage, 1000);
    setInterval(createHeart, 500);

    // Phát nhạc khi người dùng chạm (nếu bị chặn)
    const music = document.getElementById("music");
    document.body.addEventListener("click", () => {
      music.play().catch(() => {});
    });
  </script>
</body>
</html>

